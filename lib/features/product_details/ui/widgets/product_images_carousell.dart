import 'package:flutter/material.dart';

class ProductImagesCarousell extends StatelessWidget {
  const ProductImagesCarousell({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
